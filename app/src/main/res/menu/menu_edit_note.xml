<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_edit_note_save"
        android:icon="@drawable/ic_save_white_24dp"
        android:title="@string/menu_edit_note_save"
        android:visible="false"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/menu_edit_note_save_disabled"
        android:icon="@drawable/ic_save_grey_24dp"
        android:title="@string/menu_edit_note_save"
        android:visible="false"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/menu_edit_note_share"
        android:icon="@drawable/ic_share_white_24dp"
        android:title="@string/menu_edit_note_share"
        app:showAsAction="never" />

</menu>
